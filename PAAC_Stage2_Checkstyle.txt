pipeline {
    agent any
    tools {
        maven 'Maven_3' // make sure to use the right tool name as specified in Jenkins
        jdk 'OracleJDK8' // make sure to use the right tool name as specified in Jenkins
    }
    stages{
        stage('Fetch code') {
          steps{
              git branch: 'paac', url:'https://github.com/infratute/devops-repo-all.git'
          }  
        }

        stage('Build') {
            steps {
                sh 'mvn clean install -DskipTests'
            }
            post {
                success {
                    echo "Now Archiving."
                    archiveArtifacts artifacts: '**/*.war'
                }
            }
        }
        stage('Test'){
            steps {
                sh 'mvn test'
            }

        }

        stage('Checkstyle Analysis'){
            steps {
                sh 'mvn checkstyle:checkstyle'
            }
        }

    }
}
